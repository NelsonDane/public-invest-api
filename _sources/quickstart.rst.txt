Quickstart
==========

Install using Pip
-----------------
Super easy, barely an inconvenience. Just run the following command:

.. code-block:: bash

    pip install public-invest-api

Then you can import the Public class and start using the API:

.. code-block:: python

    from public_invest_api import Public

    public = Public()
